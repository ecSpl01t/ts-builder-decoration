{
  "name": "ts-decoration-builder-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Typescript builder decoration plugin",
  "main": "src/index.js",
  "scripts": {
    "build": "tsfmt -r && tsc",
    "test": "TSS_LOG='-file tsserver.log -level verbose' node node_modules/typescript/bin/tsserver < input.ldjson",
    "prepublish": "echo You should exec 'npm publish --access=public'"
  },
  "dependencies": {},
  "devDependencies": {
    "@vvakame/typescript-plugin-example": "^1.1.1",
    "typescript": "^3.2.2",
    "typescript-formatter": "^7.2.2",
    "typescript-template-language-service-decorator": "^2.2.0"
  }
}
